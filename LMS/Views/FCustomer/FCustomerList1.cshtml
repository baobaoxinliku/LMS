@model PagedList.IPagedList<LMS.Models.FCustomer>
@using PagedList.Mvc
@{
    Layout = "~/Views/Shared/MyLayout.cshtml";
}
@section TitleSection{
    FCustomerList
}
@section HeaderSection{

}
@section ContentBody{
    <div id="content">
        <div id="content-header">
            <div id="breadcrumb">
                <a href="#" title="返回首页" class="tip-bottom"><i class="icon-home"></i>首页</a>
                <a href="#">客户情况</a>
                <a href="#">发货人信息</a>
                <a href="#" class="current">发货人列表</a>
            </div>
            <h1>用户列表</h1><hr />
        </div>
        <div class="container">
            @using (Html.BeginForm())
            {
                <p>
                    请输入您要搜索的姓名 ： @Html.TextBox("Search")
                    <button type="submit" class="tip-bottom" title="Search"><i class="icon-search icon-white"></i></button>
                </p>
            }
            <div class="widget-box">
                <div class="widget-title">
                    <span class="icon"><i class="icon-th"></i></span>
                    <h5>发货人信息表</h5>
                </div>
                <div class="widget-content nopadding">
                    <table class="table table-bordered data-table">
                        <thead>
                            <tr>
                                <th>发货人编号</th>
                                <th>发货人姓名</th>
                                <th>发货人电话</th>
                                <th></th>
                                <th></th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (LMS.Models.FCustomer item in Model)
                            {
                                <tr>
                                    <td>@item.FCustoID</td>
                                    <td>@item.FCustoName</td>
                                    <td>@item.FCustoTel</td>
                                    <td>@Html.ActionLink("详情", "FCustomerDetail", new { id = item.FCustoID })</td>
                                    <td>@Html.ActionLink("编辑", "FCustomerEdit", new { id = item.FCustoID })</td>
                                    <td>@Html.ActionLink("删除", "FCustomerDelete", new { id = item.FCustoID })</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <div>
                        每页 @Model.PageSize 条记录，共 @Model.PageCount 页，当前第 @Model.PageNumber 页
                        @Html.PagedListPager(Model, page => Url.Action("IndexUser1", new { page }))
                    </div>
                </div>
            </div>
        </div>
    </div>
}
